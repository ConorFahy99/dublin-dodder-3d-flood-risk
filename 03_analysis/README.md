# Analysis scripts

Python scripts for data processing. Run in order (01, 02, ...).

Dependencies: `geopandas`, `rasterio`, `shapely`, `numpy`

Install: `pip install geopandas rasterio shapely numpy`
