# Processed data

Outputs of `03_analysis/` scripts. Generated programmatically — regenerate from raw data rather than editing by hand.

| File | Description |
|---|---|
| `dodder_catchment.gpkg` | Study area catchment boundary (EPSG:2157) |
| `buildings.gpkg` | Building footprints with height and inundation depth attributes |
| `flood_depth.tif` | Depth grid per flood scenario |
